#include<stdio.h>

void b()
{
	
	int l, w, s;
	printf("Please  entry the length and width of rectangle\n");
	scanf_s("%d%d",&l, &w);
	s = l * w;
	printf("The rectangle's area is %d", s);
	
}